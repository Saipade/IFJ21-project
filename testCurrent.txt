require "ifj21"

global notmain : function (integer)

function main()
	
	local a : integer = 21
	notmain(a)
end

main()

function notmain( a : integer) 

	while (a > 0) do
		write (a, "\n")
		a = a - 1
	end

end